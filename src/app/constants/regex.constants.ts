export const regexConstants = {
    array: {
        ofNumbers: /^\[(( )*[0-9]+( )*,)*(( )*[0-9]+( )*)?\]$/,
        ofStrings: /^\[(( )*\"[A-z0-9 ]+\"( )*,)*(( )*\"[A-z0-9 ]+\"( )*)?\]$/,
        ofStringsAndNumbers: /(^\[(( )*[0-9]+( )*,)*(( )*[0-9]+( )*)?\]$)|(^\[(( )*\"[A-z0-9 ]+\"( )*,)*(( )*\"[A-z0-9 ]+\"( )*)?\]$)/,
    }
};